import React from "react";
import "./Button.css";

const TestButton = ({ label, value, openTest, setOpenTest }) => {
  return (
    <div></div>
    // <div className="button" onClick={() => setOpenTest(!openTest)}>
    //   {label}
    // </div>
  );
};

export default TestButton;
